import React from "react";
import "./CartList.css";
function CartList({ products }) {
  return (
    <main>
      <ul>
        {products.map((product) => (
          <h2>asd</h2>
        ))}
      </ul>
    </main>
  );
}

export default CartList;
